# Primary: OpenRouter powers all text roles + image generation
OPENROUTER_API_KEY=

# Optional free fallbacks (used if OpenRouter is unreachable or out of credits)
GROQ_API_KEY=
GEMINI_API_KEY=

# Optional: higher Pollinations limits / fewer watermarks (image fallback)
# POLLINATIONS_API_KEY=
